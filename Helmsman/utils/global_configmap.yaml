## The data here is of generic interest to modules in different namespaces hence this is marked as global.
## Replace your domain names here.
## mosip-api-host:  External public access. (Typically required only in production rollouts).
## mosip-api-internal-host: Internal secure access over Wireguard.
## By default all domains and subdomains listed below point to mosip-api-internal-host. Modify this default behavior ONLY in production rollout as follows:
##     mosip-prereg-host:  Points to mosip-api-host.
##     mosip-pmp-host: Points to mosip-api-host.

apiVersion: v1
kind: ConfigMap
metadata:
  name: global
  namespace: default
data:
  installation-name: ${ENV}
  installation-domain: ${ENV}
  mosip-version: ${VERSION}
  mosip-api-host: api.${ENV}
  mosip-api-internal-host: api-internal.${ENV}
  mosip-prereg-host: prereg.${ENV}
  mosip-activemq-host: activemq.${ENV}
  mosip-kibana-host: kibana.${ENV}
  mosip-admin-host: admin.${ENV}
  mosip-regclient-host: regclient.${ENV}
  mosip-minio-host: minio.${ENV}
  mosip-kafka-host: kafka.${ENV}
  mosip-iam-external-host: iam.${ENV}
  mosip-postgres-host: postgres.${ENV}
  mosip-pmp-host: pmp.${ENV}
  mosip-resident-host: resident.${ENV}
  mosip-compliance-host: compliance.${ENV}
  mosip-esignet-host: esignet.${ENV}
  mosip-smtp-host: smtp.${ENV}
  mosip-pmp-revamp-ui-host: pmp-revamp.${ENV}
  mosip-signup-host: signup.${ENV}
  is_glowroot_env: absent
