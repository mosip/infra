# Default values for dummy-postgres-setup
# This is a minimal chart that creates ConfigMaps for postgres setup

# Override the chart name
nameOverride: ""
fullnameOverride: ""

# Common labels and annotations (optional)
commonLabels: {}
commonAnnotations: {}

# PostgreSQL ConfigMaps configuration
postgres:
  configmaps:
    # Example configmap - you can add more or modify as needed
    postgres-setup-config:
      postgres-host: ""
      postgres-port: ""


    # Add more configmaps here if needed
    # postgres-secrets-config:
    #   secret-key: "value"
    #   database-url: "postgresql://localhost:5432"
