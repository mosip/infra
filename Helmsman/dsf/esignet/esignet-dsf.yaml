# =============================================================================
# eSignet Profile - eSignet Services DSF (Desired State File)
# =============================================================================
# This DSF deploys eSignet v1.7.1 services for the standalone eSignet profile.
#
# Components (in priority order):
#   1. Keycloak Init (eSignet-specific clients and roles)
#   2. eSignet service v1.7.1
#   3. OIDC UI v1.7.1
#   4. Mock Identity System (optional, disabled by default)
#   5. Mock Relying Party Service (optional)
#   6. Mock Relying Party UI (optional)
#   7. Partner Onboarder (eSignet + Resident OIDC)
#   8. Demo OIDC Partner Onboarder
#
# Based on eSignet v1.7.1 deploy scripts:
#   - install-esignet.sh
#   - initialise-prereq.sh (keycloak-init)
# =============================================================================

helmDefaults:
  tillerNamespace: kube-system
  tillerless: true
  install: true

helmRepos:
  bitnami: https://charts.bitnami.com/bitnami
  mosip: https://mosip.github.io/mosip-helm

namespaces:
  esignet:
    protected: false
  softhsm:
    protected: false
  esignet-cre:
    protected: false
  esignet-qa11:
    protected: false
  esignet-sunbird:
    protected: false           

apps:
  # ---------------------------------------------------------------------------
  # SoftHSM for eSignet
  # ---------------------------------------------------------------------------
  # Deployed here (not in external-dsf) so esignet-softhsm-share configmap is
  # available in esignet namespace before eSignet service starts.
  esignet-softhsm:
    namespace: esignet
    enabled: true
    version: 12.0.1
    chart: mosip/softhsm
    valuesFile: "$WORKDIR/utils/softhsm-esignet-values.yaml"
    timeout: 480
    priority: -16
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/softhsm-esignet-setup.sh"

  esignet-softhsm-cre:
    namespace: esignet-cre
    enabled: true
    version: 12.0.1
    chart: mosip/softhsm
    valuesFile: "$WORKDIR/utils/softhsm-esignet-cre-values.yaml"
    timeout: 480
    priority: -16
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/softhsm-esignet-cre-setup.sh"

  esignet-softhsm-qa11:
    namespace: esignet-qa11
    enabled: true
    version: 12.0.1
    chart: mosip/softhsm
    valuesFile: "$WORKDIR/utils/softhsm-esignet-qa11-values.yaml"
    timeout: 480
    priority: -16
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/softhsm-esignet-qa11-setup.sh"

  esignet-softhsm-sunbird:
    namespace: esignet-sunbird
    enabled: true
    version: 12.0.1
    chart: mosip/softhsm
    valuesFile: "$WORKDIR/utils/softhsm-esignet-sunbird-values.yaml"
    timeout: 480
    priority: -16
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/softhsm-esignet-sunbird-setup.sh"

  # ---------------------------------------------------------------------------
  # eSignet Keycloak Init
  # ---------------------------------------------------------------------------
  esignet-keycloak-init:
    namespace: esignet
    enabled: true
    version: 12.0.2
    chart: mosip/keycloak-init
    valuesFile: "$WORKDIR/utils/keycloak-esignet-init-values.yaml"
    set:
      keycloakExternalHost: "iam.${domain_name}"
      keycloakInternalHost: "keycloak.keycloak"
      keycloak.realms.mosip.realm_config.attributes.frontendUrl: "https://iam.${domain_name}/auth"
      clientSecrets[0].name: "${PMS_CLIENT_SECRET_KEY}"
      clientSecrets[0].secret: "${PMS_CLIENT_SECRET_VALUE}"
      clientSecrets[1].name: "${MPARTNER_DEFAULT_AUTH_SECRET_KEY}"
      clientSecrets[1].secret: "${MPARTNER_DEFAULT_AUTH_SECRET_VALUE}"
      clientSecrets[2].name: "${IDA_CLIENT_SECRET_KEY}"
      clientSecrets[2].secret: "${IDA_CLIENT_SECRET_VALUE}"
      clientSecrets[3].name: "${DEPLOYMENT_CLIENT_SECRET_KEY}"
      clientSecrets[3].secret: "${DEPLOYMENT_CLIENT_SECRET_VALUE}"
      clientSecrets[4].name: "${MPARTNER_DEFAULT_MOBILE_SECRET_KEY}"
      clientSecrets[4].secret: "${MPARTNER_DEFAULT_MOBILE_SECRET_VALUE}"
    timeout: 480
    priority: -15
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-preinstall-keycloak-init.sh"
      postInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-postinstall-keycloak-init.sh"

  # ---------------------------------------------------------------------------
  # eSignet v1.7.1
  # ---------------------------------------------------------------------------
  esignet:
    namespace: esignet
    enabled: true
    version: 0.1.0-develop
    chart: mosip/esignet
    valuesFile: "$WORKDIR/utils/esignet-plugin-values.yaml"
    set:
      image.repository: "mosipqa/esignet-with-plugins"
      image.tag: "1.8.x"
      enable_insecure: "false"
      springConfigNameEnv: ""
      activeProfileEnv: ""
      pluginNameEnv: "esignet-mock-plugin.jar"
      pluginUrlEnv: ""
      extraEnvVarsCM[0]: "esignet-softhsm-share"
      #extraEnvVarsCM[1]: "kafka-config"
      metrics.serviceMonitor.enabled: "false"
      domainConfig.MOSIP_ESIGNET_HOST: "esignet.${domain_name}"
      domainConfig.MOSIP_SIGNUP_HOST: "signup.${domain_name}"
      domainConfig.MOSIP_API_HOST: "api.${domain_name}"
      domainConfig.MOSIP_IAM_EXTERNAL_HOST: "iam.${domain_name}"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
      domainConfig.MOSIP_KAFKA_HOST: "kafka.${domain_name}"
      domainConfig.MOSIP_POSTGRES_HOST: "postgres.${domain_name}"
      domainConfig.MOSIP_SMTP_HOST: "smtp.${domain_name}"
      domainConfig.installation-domain: "${domain_name}"
    wait: true
    timeout: 600
    priority: -14
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-preinstall.sh"

  esignet-cre:
    namespace: esignet-cre
    enabled: true
    version: 0.1.0-develop
    chart: mosip/esignet
    valuesFile: "$WORKDIR/utils/esignet-cre-plugin-values.yaml"
    set:
      image.repository: "mosipqa/esignet-with-plugins"
      image.tag: "1.8.x"
      enable_insecure: "false"
      springConfigNameEnv: ""
      activeProfileEnv: ""
      pluginNameEnv: "mosip-identity-plugin.jar"
      pluginUrlEnv: ""
      extraEnvVarsCM[0]: "esignet-softhsm-share"
      #extraEnvVarsCM[1]: "kafka-config"
      metrics.serviceMonitor.enabled: "false"
      domainConfig.MOSIP_ESIGNET_HOST: "esignet-cre.${domain_name}"
      domainConfig.MOSIP_SIGNUP_HOST: "signup-cre.${domain_name}"
      domainConfig.MOSIP_API_HOST: "api.${domain_name}"
      domainConfig.MOSIP_IAM_EXTERNAL_HOST: "iam.${domain_name}"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
      domainConfig.MOSIP_KAFKA_HOST: "kafka.${domain_name}"
      domainConfig.MOSIP_POSTGRES_HOST: "postgres.${domain_name}"
      domainConfig.MOSIP_SMTP_HOST: "smtp.${domain_name}"
      domainConfig.installation-domain: "${domain_name}"
    wait: true
    timeout: 600
    priority: -14
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-cre-preinstall.sh"

  esignet-qa11:
    namespace: esignet-qa11
    enabled: true
    version: 0.1.0-develop
    chart: mosip/esignet
    valuesFile: "$WORKDIR/utils/esignet-qa11-plugin-values.yaml"
    set:
      image.repository: "mosipqa/esignet-with-plugins"
      image.tag: "1.8.x"
      enable_insecure: "false"
      springConfigNameEnv: ""
      activeProfileEnv: ""
      pluginNameEnv: "mosip-identity-plugin.jar"
      pluginUrlEnv: ""
      extraEnvVarsCM[0]: "esignet-softhsm-share"
      #extraEnvVarsCM[1]: "kafka-config"
      metrics.serviceMonitor.enabled: "false"
      domainConfig.MOSIP_ESIGNET_HOST: "esignet-qa11.${domain_name}"
      domainConfig.MOSIP_SIGNUP_HOST: "signup-qa11.${domain_name}"
      domainConfig.MOSIP_API_HOST: "api.${domain_name}"
      domainConfig.MOSIP_IAM_EXTERNAL_HOST: "iam.${domain_name}"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
      domainConfig.MOSIP_KAFKA_HOST: "kafka.${domain_name}"
      domainConfig.MOSIP_POSTGRES_HOST: "postgres.${domain_name}"
      domainConfig.MOSIP_SMTP_HOST: "smtp.${domain_name}"
      domainConfig.installation-domain: "${domain_name}"
    wait: true
    timeout: 600
    priority: -14
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-qa11-preinstall.sh"

  esignet-sunbird:
    namespace: esignet-sunbird
    enabled: true
    version: 0.1.0-develop
    chart: mosip/esignet
    valuesFile: "$WORKDIR/utils/esignet-sunbird-plugin-values.yaml"
    set:
      image.repository: "mosipqa/esignet-with-plugins"
      image.tag: "1.8.x"
      enable_insecure: "false"
      springConfigNameEnv: ""
      activeProfileEnv: ""
      pluginNameEnv: "sunbird-rc-plugin.jar"
      pluginUrlEnv: ""
      extraEnvVarsCM[0]: "esignet-softhsm-share"
      #extraEnvVarsCM[1]: "kafka-config"
      metrics.serviceMonitor.enabled: "false"
      domainConfig.MOSIP_ESIGNET_HOST: "esignet-sunbird.${domain_name}"
      domainConfig.MOSIP_SIGNUP_HOST: "signup-sunbird.${domain_name}"
      domainConfig.MOSIP_API_HOST: "api.${domain_name}"
      domainConfig.MOSIP_IAM_EXTERNAL_HOST: "iam.${domain_name}"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
      domainConfig.MOSIP_KAFKA_HOST: "kafka.${domain_name}"
      domainConfig.MOSIP_POSTGRES_HOST: "postgres.${domain_name}"
      domainConfig.MOSIP_SMTP_HOST: "smtp.${domain_name}"
      domainConfig.installation-domain: "${domain_name}"
    wait: true
    timeout: 600
    priority: -14
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-sunbird-preinstall.sh"

  # ---------------------------------------------------------------------------
  # OIDC UI v1.7.1
  # ---------------------------------------------------------------------------
  oidc-ui:
    namespace: esignet
    enabled: true
    version: 0.1.0-develop
    chart: mosip/oidc-ui
    set:
      image.repository: "mosipqa/oidc-ui"
      image.tag: "1.8.x"    
      istio.hosts[0]: "esignet.${domain_name}"
      oidc_ui.oidc_service_host: "esignet.esignet"
      oidc_ui.configmaps.oidc-ui.REACT_APP_API_BASE_URL: "http://esignet.esignet/v1/esignet"
      oidc_ui.configmaps.oidc-ui.REACT_APP_SBI_DOMAIN_URI: "http://esignet.esignet"
    timeout: 1200
    priority: -13
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/oidc-ui-preinstall.sh"

  oidc-ui-cre:
    namespace: esignet-cre
    enabled: true
    version: 0.1.0-develop
    chart: mosip/oidc-ui
    set:
      image.repository: "mosipqa/oidc-ui"
      image.tag: "1.8.x"
      istio.hosts[0]: "esignet-cre.${domain_name}"
      oidc_ui.oidc_service_host: "esignet-cre.esignet-cre"
      oidc_ui.configmaps.oidc-ui.REACT_APP_API_BASE_URL: "http://esignet-cre.esignet-cre/v1/esignet"
      oidc_ui.configmaps.oidc-ui.REACT_APP_SBI_DOMAIN_URI: "http://esignet-cre.esignet-cre"
    timeout: 1200
    priority: -13
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/oidc-ui-cre-preinstall.sh"

  oidc-ui-qa11:
    namespace: esignet-qa11
    enabled: true
    version: 0.1.0-develop
    chart: mosip/oidc-ui
    set:
      image.repository: "mosipqa/oidc-ui"
      image.tag: "1.8.x"
      istio.hosts[0]: "esignet-qa11.${domain_name}"
      oidc_ui.oidc_service_host: "esignet-qa11.esignet-qa11"
      oidc_ui.configmaps.oidc-ui.REACT_APP_API_BASE_URL: "http://esignet-qa11.esignet-qa11/v1/esignet"
      oidc_ui.configmaps.oidc-ui.REACT_APP_SBI_DOMAIN_URI: "http://esignet-qa11.esignet-qa11"
    timeout: 1200
    priority: -13
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/oidc-ui-qa11-preinstall.sh"

  oidc-ui-sunbird:
    namespace: esignet-sunbird
    enabled: true
    version: 0.1.0-develop
    chart: mosip/oidc-ui
    set:
      image.repository: "mosipqa/oidc-ui"
      image.tag: "1.8.x"
      istio.hosts[0]: "esignet-sunbird.${domain_name}"
      oidc_ui.oidc_service_host: "esignet-sunbird.esignet-sunbird"
      oidc_ui.configmaps.oidc-ui.REACT_APP_API_BASE_URL: "http://esignet-sunbird.esignet-sunbird/v1/esignet"
      oidc_ui.configmaps.oidc-ui.REACT_APP_SBI_DOMAIN_URI: "http://esignet-sunbird.esignet-sunbird"
    timeout: 1200
    priority: -13
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/oidc-ui-sunbird-preinstall.sh"

  # ---------------------------------------------------------------------------
  # Mock Identity System (Optional)
  # ---------------------------------------------------------------------------
  # Enable for testing with mock identity data.
  softhsm-mock-identity-system:
    namespace: softhsm
    enabled: true
    version: 12.0.1
    chart: mosip/softhsm
    valuesFile: "$WORKDIR/utils/softhsm-mock-identity-system-values.yaml"
    wait: true
    timeout: 480
    priority: -11
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/softhsm-mock-identity-system-preinstall.sh"
      postInstall: "$WORKDIR/hooks/esignet-1.7.1/softhsm-mock-identity-system-postinstall.sh"

  mock-identity-system:
    namespace: esignet
    enabled: true
    version: 0.1.0-develop
    chart: mosip/mock-identity-system
    set:
      enable_insecure: "false"
      extraEnvVarsCM[0]: "esignet-softhsm-share"
      extraEnvVarsCM[1]: "softhsm-mock-identity-system-share"
      domainConfig.MOSIP_ESIGNET_HOST: "esignet.${domain_name}"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
    timeout: 480
    priority: -11
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-identity-system-preinstall.sh"

  # ---------------------------------------------------------------------------
  # Mock Relying Party (Optional)
  # ---------------------------------------------------------------------------
  mock-relying-party-service:
    namespace: esignet
    enabled: true
    version: 1.7.1-develop
    chart: mosip/mock-relying-party-service
    set:
      mock_relying_party_service.ESIGNET_SERVICE_URL: "http://esignet.esignet/v1/esignet"
      mock_relying_party_service.ESIGNET_AUD_URL: "https://esignet.${domain_name}/v1/esignet/oauth/v2/token"
      enable_insecure: "false"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
    timeout: 480
    priority: -10
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-service-preinstall.sh"

  mock-relying-party-service-cre:
    namespace: esignet-cre
    enabled: true
    version: 1.7.1-develop
    chart: mosip/mock-relying-party-service
    set:
      mock_relying_party_service.ESIGNET_SERVICE_URL: "http://esignet-cre.esignet-cre/v1/esignet"
      mock_relying_party_service.ESIGNET_AUD_URL: "https://esignet-cre.${domain_name}/v1/esignet/oauth/v2/token"
      enable_insecure: "false"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
    timeout: 480
    priority: -10
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-service-cre-preinstall.sh"

  mock-relying-party-service-qa11:
    namespace: esignet-qa11
    enabled: true
    version: 1.7.1-develop
    chart: mosip/mock-relying-party-service
    set:
      mock_relying_party_service.ESIGNET_SERVICE_URL: "http://esignet-qa11.esignet-qa11/v1/esignet"
      mock_relying_party_service.ESIGNET_AUD_URL: "https://esignet-qa11.${domain_name}/v1/esignet/oauth/v2/token"
      enable_insecure: "false"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
    timeout: 480
    priority: -10
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-service-qa11-preinstall.sh"

  mock-relying-party-service-sunbird:
    namespace: esignet-sunbird
    enabled: true
    version: 1.7.1-develop
    chart: mosip/mock-relying-party-service
    set:
      mock_relying_party_service.ESIGNET_SERVICE_URL: "http://esignet-sunbird.esignet-sunbird/v1/esignet"
      mock_relying_party_service.ESIGNET_AUD_URL: "https://esignet-sunbird.${domain_name}/v1/esignet/oauth/v2/token"
      enable_insecure: "false"
      domainConfig.MOSIP_API_INTERNAL_HOST: "api-internal.${domain_name}"
    timeout: 480
    priority: -10
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-service-sunbird-preinstall.sh"

  mock-relying-party-ui:
    namespace: esignet
    enabled: true
    version: 0.1.0-develop
    chart: mosip/mock-relying-party-ui
    set:
      mock_relying_party_ui.mock_relying_party_ui_service_host: "healthservices.${domain_name}"
      mock_relying_party_ui.ESIGNET_UI_BASE_URL: "https://esignet.${domain_name}"
      mock_relying_party_ui.MOCK_RELYING_PARTY_SERVER_URL: "https://healthservices.${domain_name}/mock-relying-party-service"
      mock_relying_party_ui.REDIRECT_URI: "https://healthservices.${domain_name}/userprofile"
      mock_relying_party_ui.REDIRECT_URI_REGISTRATION: "https://healthservices.${domain_name}/registration"
      mock_relying_party_ui.SIGN_IN_BUTTON_PLUGIN_URL: "https://esignet.${domain_name}/plugins/sign-in-button-plugin.js"
      istio.hosts[0]: "healthservices.${domain_name}"
    wait: true
    timeout: 480
    priority: -9
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-ui-preinstall.sh"

  mock-relying-party-ui-cre:
    namespace: esignet-cre
    enabled: true
    version: 0.1.0-develop
    chart: mosip/mock-relying-party-ui
    set:
      mock_relying_party_ui.mock_relying_party_ui_service_host: "healthservices-cre.${domain_name}"
      mock_relying_party_ui.ESIGNET_UI_BASE_URL: "https://esignet-cre.${domain_name}"
      mock_relying_party_ui.MOCK_RELYING_PARTY_SERVER_URL: "https://healthservices-cre.${domain_name}/mock-relying-party-service"
      mock_relying_party_ui.REDIRECT_URI: "https://healthservices-cre.${domain_name}/userprofile"
      mock_relying_party_ui.REDIRECT_URI_REGISTRATION: "https://healthservices-cre.${domain_name}/registration"
      mock_relying_party_ui.SIGN_IN_BUTTON_PLUGIN_URL: "https://esignet-cre.${domain_name}/plugins/sign-in-button-plugin.js"
      istio.hosts[0]: "healthservices-cre.${domain_name}"
    wait: true
    timeout: 480
    priority: -9
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-ui-cre-preinstall.sh"

  mock-relying-party-ui-qa11:
    namespace: esignet-qa11
    enabled: true
    version: 0.1.0-develop
    chart: mosip/mock-relying-party-ui
    set:
      mock_relying_party_ui.mock_relying_party_ui_service_host: "healthservices-qa11.${domain_name}"
      mock_relying_party_ui.ESIGNET_UI_BASE_URL: "https://esignet-qa11.${domain_name}"
      mock_relying_party_ui.MOCK_RELYING_PARTY_SERVER_URL: "https://healthservices-qa11.${domain_name}/mock-relying-party-service"
      mock_relying_party_ui.REDIRECT_URI: "https://healthservices-qa11.${domain_name}/userprofile"
      mock_relying_party_ui.REDIRECT_URI_REGISTRATION: "https://healthservices-qa11.${domain_name}/registration"
      mock_relying_party_ui.SIGN_IN_BUTTON_PLUGIN_URL: "https://esignet-qa11.${domain_name}/plugins/sign-in-button-plugin.js"
      istio.hosts[0]: "healthservices-qa11.${domain_name}"
    wait: true
    timeout: 480
    priority: -9
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-ui-qa11-preinstall.sh"

  mock-relying-party-ui-sunbird:
    namespace: esignet-sunbird
    enabled: true
    version: 0.1.0-develop
    chart: mosip/mock-relying-party-ui
    set:
      mock_relying_party_ui.mock_relying_party_ui_service_host: "healthservices-sunbird.${domain_name}"
      mock_relying_party_ui.ESIGNET_UI_BASE_URL: "https://esignet-sunbird.${domain_name}"
      mock_relying_party_ui.MOCK_RELYING_PARTY_SERVER_URL: "https://healthservices-sunbird.${domain_name}/mock-relying-party-service"
      mock_relying_party_ui.REDIRECT_URI: "https://healthservices-sunbird.${domain_name}/userprofile"
      mock_relying_party_ui.REDIRECT_URI_REGISTRATION: "https://healthservices-sunbird.${domain_name}/registration"
      mock_relying_party_ui.SIGN_IN_BUTTON_PLUGIN_URL: "https://esignet-sunbird.${domain_name}/plugins/sign-in-button-plugin.js"
      istio.hosts[0]: "healthservices-sunbird.${domain_name}"
    wait: true
    timeout: 480
    priority: -9
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/mock-relying-party-ui-sunbird-preinstall.sh"

  # ---------------------------------------------------------------------------
  # Partner Onboarders
  # ---------------------------------------------------------------------------
  # Two onboarders for eSignet standalone — enable the one matching your plugin:
  #   esignet-mock-rp-onboarder: for mock plugin (plugin 1) and sunbird-rc (plugin 3)
  #   esignet-misp-onboarder:    for mosip-identity-plugin (plugin 2) only

  # eSignet MISP Partner Onboarder
  # Onboards the eSignet MISP client — only needed with mosip-identity-plugin (plugin 2).
  # Runs FIRST so the MISP license key is written before mock-rp-onboarder starts.
  # After completion, restarts the esignet deployment to pick up the new MISP license key.
  esignet-misp-onboarder:
    namespace: esignet
    enabled: false
    version: 0.1.0-develop
    chart: mosip/partner-onboarder
    set:
      onboarding.modules[0].name: "esignet"
      onboarding.modules[0].enabled: "true"
      onboarding.configmaps.s3.s3-host: "http://minio.minio:9000"
      onboarding.configmaps.s3.s3-user-key: "admin"
      onboarding.configmaps.s3.s3-region: ""
      onboarding.configmaps.s3.s3-bucket-name: "onboarder"
      onboarding.secrets.s3.s3-user-secret: "6is3EAODT0"
      onboarding.configmaps.onboarder-namespace.ns_esignet: "esignet"
      onboarding.variables.push_reports_to_s3: "true"
      onboarding.variables.mosipid: "false"
      extraEnvVarsCM[0]: "keycloak-env-vars"
      extraEnvVarsCM[1]: "keycloak-host"
      extraEnvVarsSecret[0]: "keycloak"
      extraEnvVarsSecret[1]: "keycloak-client-secrets"
      domainConfig.mosip-api-internal-host: "api-internal.${cre_domain_name}"
      domainConfig.mosip-api-host: "api.${domain_name}"
      domainConfig.mosip-esignet-host: "esignet.${domain_name}"
      domainConfig.mosip-esignet-insurance-host: "esignet-insurance.${domain_name}"
      domainConfig.mosip-resident-host: "resident.${domain_name}"
      domainConfig.installation-domain: "${domain_name}"
    wait: true
    waitForJobs: true
    timeout: 600
    priority: -6
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-misp-onboarder-preinstall.sh"
      preUpgrade: "$WORKDIR/hooks/esignet-1.7.1/esignet-misp-onboarder-preinstall.sh"
      postInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-misp-onboarder-postinstall.sh"
      postUpgrade: "$WORKDIR/hooks/esignet-1.7.1/esignet-misp-onboarder-postinstall.sh"

  # Mock RP OIDC Partner Onboarder
  # Onboards the mock relying party as an OIDC client.
  # Enable for plugin 1 (mock) or plugin 3 (sunbird-rc). Set mosipid: "true" for mosip-identity-plugin.
  esignet-mock-rp-onboarder:
    namespace: esignet
    enabled: true
    version: 0.1.0-develop
    chart: mosip/partner-onboarder
    set:
      onboarding.modules[0].name: "mock-rp-oidc"
      onboarding.modules[0].enabled: "true"
      onboarding.configmaps.s3.s3-host: "http://minio.minio:9000"
      onboarding.configmaps.s3.s3-user-key: "admin"
      onboarding.configmaps.s3.s3-region: ""
      onboarding.secrets.s3.s3-user-secret: "6is3EAODT0"
      onboarding.configmaps.s3.s3-bucket-name: "onboarder"
      onboarding.variables.mosipid: "false"
      onboarding.variables.push_reports_to_s3: "true"
      extraEnvVarsCM[0]: "keycloak-env-vars"
      extraEnvVarsCM[1]: "keycloak-host"
      domainConfig.mosip-api-internal-host: "api-internal.${domain_name}"
      domainConfig.mosip-api-host: "api.${domain_name}"
      domainConfig.mosip-esignet-host: "esignet.${domain_name}"
      domainConfig.mosip-esignet-insurance-host: "esignet-insurance.${domain_name}"
      domainConfig.mosip-resident-host: "resident.${domain_name}"
      domainConfig.installation-domain: "${domain_name}"
    wait: true
    waitForJobs: true
    timeout: 600
    priority: -5
    hooks:
      preInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-mock-rp-onboarder-preinstall.sh"
      preUpgrade: "$WORKDIR/hooks/esignet-1.7.1/esignet-mock-rp-onboarder-preinstall.sh"
      postInstall: "$WORKDIR/hooks/esignet-1.7.1/esignet-mock-rp-onboarder-postinstall.sh"
      postUpgrade: "$WORKDIR/hooks/esignet-1.7.1/esignet-mock-rp-onboarder-postinstall.sh"

      